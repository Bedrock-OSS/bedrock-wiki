import{o as e,c as o,a}from"./404.md.bd586e43.js";const t='{"title":"MoLang","description":"","frontmatter":{"title":"MoLang","tags":["intermediate"]},"headers":[{"level":2,"title":"**MoLang**","slug":"molang"},{"level":3,"title":"| Introduction","slug":"introduction"},{"level":3,"title":"| Accessing Values","slug":"accessing-values"},{"level":3,"title":"| Handling values","slug":"handling-values"}],"relativePath":"concepts/molang.md","lastUpdated":1635345949116}',n={},i=a('<h2 id="molang"><a class="header-anchor" href="#molang" aria-hidden="true">#</a> <strong>MoLang</strong></h2><h3 id="introduction"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> | Introduction</h3><p>Pretty much everything evaluates to a number; if something doesn&#39;t evaluate to a number, you can use an <code>operator</code> to make it into one. You can basically just think of MoLang as one big math equation.</p><p>An equation evaluates to <code>true</code> when any number except <code>0</code> is returned. When I reference <code>returning</code>, I&#39;m talking about the output of an equation. There is also a <code>return</code> statement, but I don&#39;t usually use it, and will therefore not be talking about it.</p><h3 id="accessing-values"><a class="header-anchor" href="#accessing-values" aria-hidden="true">#</a> | Accessing Values</h3><p>There are three main ways to access and use values in MoLang (queries, variables and temp variables)</p><ul><li><p><strong>Queries</strong> are read only values returned by the game. You cannot set these values, only read them. (<code>query.example_query</code> | <code>q.example_query</code>)</p></li><li><p><strong>Variables</strong> are read and write values that you can manipulate, these can be set and read through MoLang. (<code>variable.example_variable</code> | <code>v.example_variable</code>)</p><ul><li>There are also hard-coded variables which act practically the same way as queries, but can only be used in certain situations.</li></ul></li><li><p><strong>Temp. Variables</strong> are practically the same as variables, except they only exist in the current scope. (<code>temp.example_temp</code> | <code>t.example_temp</code>)</p><ul><li>A &quot;scope&quot; can refer to the current <code>for_each</code> or <code>loop</code> <em>or</em> just the current expression, if it&#39;s not used within either</li></ul></li></ul><h3 id="handling-values"><a class="header-anchor" href="#handling-values" aria-hidden="true">#</a> | Handling values</h3><ul><li><p><strong>Logical Operators</strong> can be used to convert non-numbers into 1s or 0s. These include: <code>==</code>, <code>!=</code>, <code>&lt;</code>, <code>&gt;</code>, <code>&lt;=</code>, <code>&gt;=</code>.</p><ul><li><p>Example.) &quot;<code>query.get_equipped_item_name == &#39;stick&#39;</code>&quot; Will evaluate to <code>1</code>/<code>true</code> when holding a stick</p></li><li><p>There is also a <em>second</em> set of <em>Logical Operators</em> which can be used to &#39;group&#39; values into <code>and/or</code> statements, often used in cases where you need <em>multiple</em> things to evaluate to <code>true</code> or just <em>one out of many</em>. <code>&amp;&amp;</code> represents an <code>and</code> statement, and <code>||</code> represents an <code>or</code> statement.</p><ul><li>Example.) &quot;<code>query.is_sneaking &amp;&amp; query.is_using_item</code>&quot; Will evaluate to <code>1</code>/<code>true</code> when sneaking <em>and</em> using an item</li><li>Example.) &quot;<code>query.is_sneaking || query.is_jumping</code>&quot; // Evaluates to <code>1</code>/<code>true</code> when either jumping <em>or</em> sneaking</li></ul></li></ul></li><li><p><strong>Parentheses</strong>, <code>( )</code>, are also a major help when grouping values or performing math operations.</p><ul><li>Example.) &quot;<code>query.is_sneaking &amp;&amp; (query.get_equipped_item_name == </code>stick<code>|| query.get_equipped_item_name ==</code>diamond<code>)</code>&quot; Will evaluate to <code>1</code>/<code>true</code> when sneaking <em>and</em> holding either a stick <em>or</em> a diamond</li></ul></li><li><p><strong>Conditional Operators</strong> can be used as <code>if/else</code> statements.</p><ul><li>A <em>binary</em> conditional operator refers to just using <code>?</code>. When this is used, it&#39;ll output your value or <code>0</code> depending on whether the given input value is <code>true</code>. <ul><li>Example.) &quot;<code>query.is_sneaking ? 5</code>&quot; Will output a <code>5</code> when sneaking, otherwise returning a <code>0</code></li></ul></li><li>A <em>trinary</em> conditional operator refers to using <code>?</code> and <code>:</code>. When this is used, it&#39;ll output one of the two given values depending on whether your given input value is <code>true</code>. <ul><li>Example.) &quot;<code>query.is_sneaking ? 10 : 3</code>&quot; Will output a <code>10</code> when sneaking, otherwise returning a <code>3</code></li></ul></li></ul></li></ul>',9);n.render=function(a,t,n,d,r,l){return e(),o("div",null,[i])};export default n;export{t as __pageData};
