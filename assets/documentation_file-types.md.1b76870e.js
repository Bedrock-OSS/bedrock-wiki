import{_ as e,c as o,o as a,e as t}from"./404.md.f0d453c2.js";const f=JSON.parse('{"title":"File Types","description":"","frontmatter":{"title":"File Types","max_toc_level":3,"mentions":["Ciosciaa","SirLich"]},"headers":[{"level":2,"title":"Worlds","slug":"worlds","link":"#worlds","children":[]},{"level":2,"title":"Projects","slug":"projects","link":"#projects","children":[]},{"level":2,"title":"Packs","slug":"packs","link":"#packs","children":[{"level":3,"title":"Behavior Packs","slug":"behavior-packs","link":"#behavior-packs","children":[]},{"level":3,"title":"Resource Packs","slug":"resource-packs","link":"#resource-packs","children":[]},{"level":3,"title":"Skin Packs","slug":"skin-packs","link":"#skin-packs","children":[]}]},{"level":2,"title":"World Templates","slug":"world-templates","link":"#world-templates","children":[]},{"level":2,"title":"Add-Ons","slug":"add-ons","link":"#add-ons","children":[]},{"level":2,"title":"Editor Add-Ons","slug":"editor-add-ons","link":"#editor-add-ons","children":[]}],"relativePath":"documentation/file-types.md"}'),r={name:"documentation/file-types.md"},c=t('<p>A number of file types exist for <em>Minecraft</em>, all for importing content. All <em>Minecraft</em> files are ZIP archives renamed to use a <code>mc\u2026</code> extension. These archives can currently be divided into three sets:</p><ul><li><strong>Levels (<code>mcworld</code> and <code>mcproject</code>)</strong>: level data and associated resources for worlds and projects</li><li><strong>Assets (<code>mcpack</code> and <code>mctemplate</code>)</strong>: cosmetics or supporting assets for worlds</li><li><strong>Composites (<code>mcaddon</code> and <code>mceditoraddon)</code></strong>: used to import up to one world or project and any number of asset types</li></ul><p>All file types for Minecraft can be opened as any file, launching Minecraft and importing the content. When packages are imported, they are automatically unpacked into their constituent files and directories. If it was not already open, most file types will launch Minecraft in normal mode; <code>mcproject</code> and <code>mceditoraddon</code> will instead launch Minecraft into Editor mode.</p><h1 id="levels" tabindex="-1">Levels <a class="header-anchor" href="#levels" aria-hidden="true">#</a></h1><p>Levels represent save data and resources for regular worlds and Editor projects. All levels, regardless of mode, are imported to <code>minecraftWorlds</code> in the <code>com.mojang</code> directory.</p><p>Importing an exact duplicate of an existing saved level will create a duplicate saved level. Composite archives will only import one level if multiple are included, including across nested composite archives.</p><h2 id="worlds" tabindex="-1">Worlds <a class="header-anchor" href="#worlds" aria-hidden="true">#</a></h2><p><code>mcworld</code> Archive encapsulating an individual world</p><p>World archives can be created a few different ways:</p><ul><li>Zipping the <em>contents</em> of a world directory and renaming the extension from <code>zip</code> to <code>mcworld</code></li><li>Using the &quot;Export World&quot; button on the Game settings screen for a world</li><li>In Editor mode, exporting the world from the File \u2192 Export as \u2192 Playable world menu option. The world will be saved to the <code>projectbackups</code> directory in the <code>com.mojang</code> folder.</li><li>In Editor mode, running the <code>/project export world</code> command. The world will be saved to the <code>projectbackups</code> directory in the <code>com.mojang</code> folder.</li></ul><p>Importing a world package while <em>Minecraft</em> is launched in Editor mode will import the world as a project. The imported world will then be inaccessible outside Editor mode and will need to be re-exported as a world for playing. Editor extension packs bundled in a world archive will be retained on import outside Editor mode.</p><h2 id="projects" tabindex="-1">Projects <a class="header-anchor" href="#projects" aria-hidden="true">#</a></h2><p><code>mcproject</code> Archive encapsulating an individual Editor project</p><p>Project archives can be created two different ways:</p><ul><li>Zipping the <em>contents</em> of a project directory and renaming the extension from <code>zip</code> to <code>mcproject</code>.</li><li>Using the &quot;Export Project&quot; button on the Game settings screen for a world</li><li>In Editor mode, running the <code>/project export project</code> command. The world will be saved to the <code>projectbackups</code> directory in the <code>com.mojang</code> folder.</li></ul><p>If <em>Minecraft</em> is not open, launching a <code>mcproject</code> file will open Editor mode. Importing a <code>mcproject</code> will fail if <em>Minecraft</em> is open but not in Editor mode.</p><h1 id="assets" tabindex="-1">Assets <a class="header-anchor" href="#assets" aria-hidden="true">#</a></h1><p>Asset archives represent a singular instance of a number of non-level contents:</p><ul><li>Behavior packs</li><li>Resource packs</li><li>Skin packs</li><li>World templates</li></ul><p>All asset archives include a manifest describing their contents. An asset archive will fail to import if its manifest UUID and version exactly matches an existing asset archive of the same type. Note that behavior and resource packs share the same UUID/version space. Behavior and resource packs self-contained within a world, project, or template will not count as duplicates for the sake of importing.</p><p>Both asset extensions, <code>mcpack</code> and <code>mctemplate</code>, appear to functionally behave the same. It&#39;s best practice to use <code>mcpack</code> for behavior, resource, and skin packs and <code>mctemplate</code> for world templates to make it more clear what&#39;s being installed. Any number of asset archives may be included in a composite archive.</p><h2 id="packs" tabindex="-1">Packs <a class="header-anchor" href="#packs" aria-hidden="true">#</a></h2><p><code>mcpack</code> Package representing an individual behavior pack, resource pack, skin pack, or world template. It&#39;s recommended only to use <code>mctemplate</code> for behavior packs, resource packs, or skin packs.</p><p>Packs are only created manually, by zipping the contents of a behavior pack, resource pack, or skin pack directory and renaming the extension from <code>zip</code> to <code>mcpack</code>. Behavior and resource packs are installed globally and do not conflict with matching behavior or resource packs installed in worlds, projects, or templates.</p><h3 id="behavior-packs" tabindex="-1">Behavior Packs <a class="header-anchor" href="#behavior-packs" aria-hidden="true">#</a></h3><p>Behavior packs are attached to servers to change or extend gameplay. Behavior packs are installed to the <code>behavior_packs</code> directory in the <code>com.mojang</code> folder.</p><p>Development behavior packs must be placed in the <code>development_behavior_packs</code> directory under <code>com.mojang</code> manually.</p><h3 id="resource-packs" tabindex="-1">Resource Packs <a class="header-anchor" href="#resource-packs" aria-hidden="true">#</a></h3><p>Resource packs are attached to clients to affect sounds, visuals, etc. Resource packs are installed to the <code>resource_packs</code> directory in the <code>com.mojang</code> folder.</p><p>Development resource packs must be placed in the <code>development_resource_packs</code> directory under <code>com.mojang</code> manually.</p><h3 id="skin-packs" tabindex="-1">Skin Packs <a class="header-anchor" href="#skin-packs" aria-hidden="true">#</a></h3><p>Skin packs are client-only packs for custom skins. Skin packs are installed to the <code>skin_packs</code> directory in the <code>com.mojang</code> folder.</p><p>Development skin packs must be placed in the <code>development_skin_packs</code> directory under <code>com.mojang</code> manually, but this feature appears non-functional.</p><h2 id="world-templates" tabindex="-1">World Templates <a class="header-anchor" href="#world-templates" aria-hidden="true">#</a></h2><p><code>mctemplate</code> Package representing an individual behavior pack, resource pack, skin pack, or world template. It&#39;s recommended only to use <code>mctemplate</code> for world templates.</p><p>World templates are installed to the <code>world_templates</code> directory under <code>com.mojang</code>. World templates can be constructed in a few different ways:</p><ul><li>Zipping the <em>contents</em> of a world directory, adding a world template manifest, and renaming the extension from <code>zip</code> to <code>mctemplate</code></li><li>In Editor mode, using the &quot;Export Template&quot; button on the Game settings screen for a world</li><li>In Editor mode, running the <code>/project export template</code> command. The world will be saved to the <code>projectbackups</code> directory in the <code>com.mojang</code> folder.</li></ul><h1 id="composites" tabindex="-1">Composites <a class="header-anchor" href="#composites" aria-hidden="true">#</a></h1><p>Composite archives are used to import up to <em>one</em> level archive and any number or combination of asset archives in a single import action. In general, contents to a composite must be packaged. Directories can also be given <em>on the top level</em> of a composite archive for importing asset types (behavior packs, resource packs, skin packs, and world templates) without needing to pre-package them. Nested sub-directories for organization may not be used.</p><p>Composite contents are treated as usual. For example, importing a <code>mcaddon</code> contianing a <code>mcworld</code> while in Editor mode will import the world as a project.</p><p>Composite archives may also contain any number or nesting of other composite archives, even across <em>Minecraft</em> modes. Nested composite archives cannot be used to get around the singular world import restriction.</p><p>Composites can only be constructed manually by zipping archives and asset types.</p><h2 id="add-ons" tabindex="-1">Add-Ons <a class="header-anchor" href="#add-ons" aria-hidden="true">#</a></h2><p><code>mcaddon</code> Generic composite content archive</p><p>Importing a <code>mcaddon</code> package while <em>Minecraft</em> is launched in Editor mode will import any contained world as a project. The imported world will then be inaccessible outside Editor mode and will need to be re-exported as a world for playing. Asset types are imported as usual.</p><h2 id="editor-add-ons" tabindex="-1">Editor Add-Ons <a class="header-anchor" href="#editor-add-ons" aria-hidden="true">#</a></h2><p><code>mceditoraddon</code> Composite content archive for Editor mode</p><p>If <em>Minecraft</em> is not open, launching a <code>mcproject</code> file will open Editor mode. Importing a <code>mcproject</code> will fail if <em>Minecraft</em> is open but not in Editor mode.</p>',48),i=[c];function d(n,s,l,p,m,h){return a(),o("div",null,i)}const k=e(r,[["render",d]]);export{f as __pageData,k as default};
